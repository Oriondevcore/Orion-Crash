# 🚀 ORION CRASH | Lead Generation PWA
**AI Amplifies. I Create.**

High-performance, Apple-quality crash betting simulation built for **ORION DEV CORE**.

## 🛠 Tech Stack
- **Frontend:** Vanilla JS / CSS3 (Hardware Accelerated) / HTML5
- **Backend:** Google Apps Script (GAS)
- **Database:** Google Sheets
- **Payments:** Yoco SDK Integration

## ⚙️ Setup Instructions
1. **Google Sheets:**
   - Create a new sheet and copy the ID.
   - Run the `initializeSystem` function in the GAS editor.
2. **Apps Script:**
   - Go to **Project Settings > Script Properties**.
   - Add: `SPREADSHEET_ID`, `YOCO_SECRET_KEY`, `DISCORD_WEBHOOK_URL`.
   - Deploy as Web App (Execute as "Me", Access "Anyone").
3. **GitHub Pages:**
   - Upload `index.html`, `manifest.json`, `sw.js`, and `404.html`.
   - Ensure `icon192.png` and `icon512.png` are in the root.
   - Enable GitHub Pages in Repository Settings.
